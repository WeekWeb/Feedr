import { SearchBarFilterPipe } from './search-bar-filter.pipe';

describe('SearchBarFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBarFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
